/* -- Modal ------------------------------------------------------------------ */
import { ThemeColors } from "@tokens/color";
import { RadiusTokens } from "@tokens/border";

export const MODAL_SIZES = ["small", "default", "large", "extraLarge"];

export const MODAL_SIZE_WIDTHS = {
  small: 264,
  default: 440,
  large: 700,
  extraLarge: 1000,
};

export const DEFAULT_MODAL_SIZE = "default";
