/**
 * Slider constants.
 */

export const DEFAULT_SLIDER_MIN = 0;
export const DEFAULT_SLIDER_MAX = 100;

// How many equal parts the track is marked into. None draws no marks.
export const DEFAULT_SLIDER_PARTITIONS = 0;
