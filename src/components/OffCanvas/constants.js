/* -- OffCanvas ---------------------------------------------------------------- */
import { ThemeColors } from "@tokens/color";
import { RadiusTokens } from "@tokens/border";

export const OFFCANVAS_PLACEMENTS = ["top", "right", "bottom", "left"];
export const OFFCANVAS_SIZES = ["default", "large"];

export const DEFAULT_OFFCANVAS_PLACEMENT = "right";
export const DEFAULT_OFFCANVAS_SIZE = "default";

