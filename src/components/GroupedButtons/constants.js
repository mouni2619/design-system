import { SpaceTokens } from "@tokens/space";

/**
 * GroupedButtons constants.
 */

// Sits on the label's own line — the design draws a 12px glyph beside 12px text.
export const GROUPED_BUTTONS_ICON_SIZE = SpaceTokens.xs;
