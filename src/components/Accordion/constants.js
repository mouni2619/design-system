/* -- Accordion -------------------------------------------------------------- */

// "bordered": one container, rows divided by hairlines. "separated": each
// row is its own bordered card with a gap between them.
export const ACCORDION_VARIANTS = ["bordered", "separated"];

export const DEFAULT_ACCORDION_VARIANT = "bordered";