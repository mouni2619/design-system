import { FontSize } from "@tokens/typography";

/**
 * Search constants.
 */

export const DEFAULT_SEARCH_SIZE = "medium";

// The magnifier sits at the size's own text size.
export const SEARCH_ICON_SIZES = {
  small: FontSize.caption,
  medium: FontSize.body,
  large: FontSize.h6,
};
