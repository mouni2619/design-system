import { SpaceTokens } from "@tokens/space";

/**
 * Breadcrumb constants.
 */

// What sits between two crumbs: the design's "/" or a chevron.
export const DEFAULT_BREADCRUMB_SEPARATOR = "slash";

// A crumb's icon sits on the label's own line — the design draws a 12px
// glyph beside 12px text — and a separator fills the same 12px slot.
export const BREADCRUMB_ICON_SIZE = SpaceTokens.xs;
