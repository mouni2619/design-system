/**
 * Shadow tokens
 *
 * Offset/blur/spread only — the color is applied by whoever consumes these,
 * so one geometry can serve every focus-ring palette.
 */

export const ShadowGeometry = {
  sm: "0 2px 4px 0",
  md: "0 4px 8px 0",
  lg: "0 8px 12px 2px",
  focus: "0 0 0 4px",
};
