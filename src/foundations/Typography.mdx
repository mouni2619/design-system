import { Meta } from "@storybook/addon-docs/blocks";
import { WeightSwatches, StyleMatrix } from "../components/TokenVisualizer";
import { TypeScale } from "../designTokens";

<Meta title="Foundations/Typography" />

# Typography

Font family — **Inter**.

## Font Scale & Metrics

<div className="my-3 table-responsive">
  <table className="table table-bordered table-hover align-middle mb-0">
    <thead className="table-light">
      <tr>
        <th scope="col" className="text-dark">Token</th>
        <th scope="col" className="text-dark">Font size</th>
        <th scope="col" className="text-dark">Line height</th>
        <th scope="col" className="text-dark">Letter spacing</th>
      </tr>
    </thead>
    <tbody>
      {TypeScale.map((t) => (
        <tr key={t.token}>
          <td className="fw-semibold text-dark">{t.token}</td>
          <td className="text-secondary">{t.fontSize}px</td>
          <td className="text-secondary">{t.lineHeight}px</td>
          <td className="text-secondary">{t.letterSpacing}px</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

## Font Weight

<WeightSwatches />

---

## Styles Matrix

<StyleMatrix />