import { Meta } from "@storybook/addon-docs/blocks";
import { WeightSwatches, StyleMatrix } from "../components/TokenViewer";
import { typeScale, typographyColors } from "../tokens";

<Meta title="Foundations/Typography" />

# Typography

Font family — **Inter**.

## Font Scale & Metrics

<div style={{ margin: "20px 0", overflowX: "auto" }}>
  <table
    style={{
      width: "100%",
      borderCollapse: "collapse",
      fontSize: "14px",
      textAlign: "left",
      fontFamily: "sans-serif",
    }}
  >
    <thead>
      <tr
        style={{
          backgroundColor: "#fafafa",
          borderBottom: "2px solid #f0f0f0",
        }}
      >
        <th style={{ padding: "12px", color: "#262626" }}>Token</th>
        <th style={{ padding: "12px", color: "#262626" }}>Font size</th>
        <th style={{ padding: "12px", color: "#262626" }}>Line height</th>
        <th style={{ padding: "12px", color: "#262626" }}>Letter spacing</th>
      </tr>
    </thead>
    <tbody>
      {typeScale.map((t) => (
        <tr key={t.token} style={{ borderBottom: "1px solid #f0f0f0" }}>
          <td style={{ padding: "12px", fontWeight: "600", color: "#1f1f1f" }}>
            {t.token}
          </td>
          <td style={{ padding: "12px", color: "#595959" }}>{t.fontSize}px</td>
          <td style={{ padding: "12px", color: "#595959" }}>
            {t.lineHeight}px
          </td>
          <td style={{ padding: "12px", color: "#595959" }}>
            {t.letterSpacing}px
          </td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

---

## Font Weight

<WeightSwatches />

---

## Styles Matrix

<StyleMatrix />

---

## Typography Colors

<div style={{ margin: "20px 0", overflowX: "auto" }}>
  <table
    style={{
      width: "100%",
      borderCollapse: "collapse",
      fontSize: "14px",
      textAlign: "left",
      fontFamily: "sans-serif",
    }}
  >
    <thead>
      <tr
        style={{
          backgroundColor: "#fafafa",
          borderBottom: "2px solid #f0f0f0",
        }}
      >
        <th style={{ padding: "12px", color: "#262626" }}>Role</th>
        <th style={{ padding: "12px", color: "#262626" }}>Reference</th>
        <th style={{ padding: "12px", color: "#262626" }}>Value</th>
      </tr>
    </thead>
    <tbody>
      {typographyColors.map(([role, ref, hex]) => (
        <tr key={role} style={{ borderBottom: "1px solid #f0f0f0" }}>
          <td style={{ padding: "12px", color: "#1f1f1f" }}>{role}</td>
          <td style={{ padding: "12px" }}>
            <code
              style={{
                background: "#f5f5f5",
                padding: "2px 6px",
                borderRadius: "4px",
                fontSize: "12px",
              }}
            >
              {ref}
            </code>
          </td>
          <td style={{ padding: "12px" }}>
            <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
              <span
                style={{
                  display: "inline-block",
                  width: "16px",
                  height: "16px",
                  backgroundColor: hex,
                  borderRadius: "3px",
                  border: "1px solid #d9d9d9",
                }}
              ></span>
              <code
                style={{
                  background: "#f5f5f5",
                  padding: "2px 6px",
                  borderRadius: "4px",
                  fontSize: "12px",
                }}
              >
                {hex}
              </code>
            </div>
          </td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
