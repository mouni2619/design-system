import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";
import { palettes, textTokens, borderTokens, backgroundTokens, iconTokens } from "../tokens";
import { ThemeTokenGrid } from "../components/TokenViewer";

<Meta title="Foundations/Color" />

# Color System

Organized into base **Palettes** and semantic **Theme Tokens**.

## Base Palettes

<ColorPalette>
  {Object.entries(palettes).map(([name, colors]) => (
    <ColorItem key={name} title={name} subtitle={`${Object.keys(colors).length} steps`} colors={colors} />
  ))}
</ColorPalette>

---

## Theme Tokens

Click on any token name or HEX value to copy it directly.

<ThemeTokenGrid title="Text Tokens" tokens={textTokens} />
<ThemeTokenGrid title="Background Tokens" tokens={backgroundTokens} />
<ThemeTokenGrid title="Border Tokens" tokens={borderTokens} />
<ThemeTokenGrid title="Icon Tokens" tokens={iconTokens} />