import { Meta } from "@storybook/addon-docs/blocks";
import { ElevationShadow, FocusRingShadow } from "@docs/blocks/shadow";

<Meta title="Foundations/Shadows" />

# Shadows

## Elevation shadows

Three soft drop shadows for layering: `shadow-sm` (cards), `shadow` (dropdowns/drawers), `shadow-lg` (modals).

<ElevationShadow />

## Focus-ring shadows

Six 4px focus halos, each using emphasis-1 of its colour ramp and paired with the matching border token.

<FocusRingShadow />
